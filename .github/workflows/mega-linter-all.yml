---
# GitHub Actions workflow for MegaLinter code quality checks
# Runs comprehensive linting and security checks on all supported file types
# https://megalinter.io/
# Flavors: https://megalinter.io/v9/flavors/
name: mega-linter-all

on:
  workflow_dispatch:

permissions: read-all

jobs:
  # Default MegaLinter (all linters)
  mega-linter:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter
        uses: oxsecurity/megalinter@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for pure C/C++ projects
  mega-linter-c_cpp:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter c_cpp
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/c_cpp@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for CI items (Dockerfile, Jenkinsfile, JSON/YAML schemas, XML)
  mega-linter-ci_light:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter ci_light
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/ci_light@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # MegaLinter for the most commonly used languages
  mega-linter-cupcake:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter cupcake
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/cupcake@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # MegaLinter for documentation projects
  mega-linter-documentation:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter documentation
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/documentation@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for C, C++, C# or VB based projects
  mega-linter-dotnet:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter dotnet
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/dotnet@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for C, C++, C# or VB based projects with JS/TS
  mega-linter-dotnetweb:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter dotnetweb
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/dotnetweb@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Contains only formatters
  mega-linter-formatters:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter formatters
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/formatters@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for GO based projects
  mega-linter-go:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter go
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/go@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for JAVA based projects
  mega-linter-java:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter java
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/java@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for JAVASCRIPT or TYPESCRIPT based projects
  mega-linter-javascript:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter javascript
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/javascript@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for PHP based projects
  mega-linter-php:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter php
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/php@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for PYTHON based projects
  mega-linter-python:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter python
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/python@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for RUBY based projects
  mega-linter-ruby:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter ruby
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/ruby@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for RUST based projects
  mega-linter-rust:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter rust
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/rust@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for Salesforce based projects
  mega-linter-salesforce:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter salesforce
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/salesforce@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for security
  mega-linter-security:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter security
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/security@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for SWIFT based projects
  mega-linter-swift:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter swift
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/swift@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optimized for TERRAFORM based projects
  mega-linter-terraform:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 💡 MegaLinter terraform
        # renovate: datasource=github-tags depName=oxsecurity/megalinter
        uses: oxsecurity/megalinter/flavors/terraform@8fbdead70d1409964ab3d5afa885e18ee85388bb # v9.4.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
